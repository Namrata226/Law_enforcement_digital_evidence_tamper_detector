version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./static/uploads:/app/static/uploads
      - ./uploads:/app/uploads
      - ./logs:/app/logs
    restart: unless-stopped
